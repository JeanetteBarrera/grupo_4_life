<!DOCTYPE html>
<html lang="es">
<%- include("./partials/head") %> 
<body>
  <!-- Header -->
    <%- include("./partials/header") %> 
  <!-- Main -->
  <main class="my-8 mx-3 sm:mx-4 md:mx-14 table-database">
    <h1 class="uppercase text-xl sm:text-2xl md:text-3xl text-center font-bold">editar / inhabilitar / eliminar</h1>
    <div class="border my-4">
      <h2 class="uppercase text-white font-bold ">IMPORTANTE!</h2>
      <h3 class="normal-case text-sm text-gray-600">Se recomienda, para mas informacion del producto, ver seccion la <span class="font-bold">DATABASE</span> que se encuentra en el <span class="font-bold">PANEL DE CONTROL</span></h3>
    </div>
    <!--contenedor de tabla-->
    <div class="table-container overflow-x-auto">
      <table class="table">
        <thead class="text-sm">
            <tr>
                <th>cod</th>
                <th>status</th>
                <th>product</th>
                <th>color</th>
                <th>link</th>
            </tr>
        </thead>
        <tbody class="text-sm">
            <%producto.forEach(data => {%>
            <%if (data.status== false) {%>
                <tr class="inactive bg-gray-200">
            <%}else{%>   
                <tr>
             <% }%>     
                <td class="data"><%=data.id%></td>  
                <td class="data">
                    <%if (data.status == true) {%>
                        <%="active"%>
                    <%}else{%>
                        <%="inactive"%>
                    <%}%>    
                </td>
                
                <td class="data"><%=data.name%></td>
                <td class="data-variante">
                    <%data.variantes.forEach(colores => {%>
                        <p><%=colores.color%></p>
                    <%});%>
                </td>
                <td class="w-1/4">
                  <a href="/admin/product/edit/<%= data.id%>" class="boton-editar ml-2 block"><i class="fas fa-pen-square"><p class="inline"> Editar</p></i></a>
                  <!--<a href="/admin/product/delete/" class="block h-1/3 p-2 my-2 bg-red-400"><i class="fas fa-trash-alt"><p>eliminar</p></i></a>-->
                  <form action="/admin/product/delete/<%= data.id %>/?_method=DELETE" method="POST">
                    <button class="boton-inhabilitar" type="submit"><i class="fas fa-low-vision"><p class="inline"> Inhabilitar</p></i></button>
                  </form>
                  <form action="#"  method="POST">
                    <button class="boton-delete" type="submit"><i class="fas fa-trash-alt"><p class="inline"> Eliminar</p></i></button>
                  </form>
                </td>
            </tr>
            <%});%>
        </tbody>
      </table>
    </div>
    <!--termina contenedor de tabla-->
  </main>
<script src="/javascripts/headerAdmin.js"></script>
</body>
</html>